item do
  expected do
    url_xpath
    url 'https://www.archlinux.org/packages/extra/x86_64/libatomic_ops/'
    xpath '//*[@id="pkgdetails"]/h2'
    regex /^libatomic_ops ([\w.]*)-\d+$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
